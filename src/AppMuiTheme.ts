import { createTheme } from "@mui/material";

export const themeMain = createTheme({
  palette: {
    primary: {
      main: "rgba(12,52,86,0.85)",
    },
    secondary: {
      main: "#f50057",
    },
  },
});
